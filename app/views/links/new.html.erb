<%= bootstrap_form_for(@link) do |f| %>
  <%= f.form_group :protocol do %>
    <%= f.radio_button :protocol, 'http', label: "HTTP", inline: true, checked: true %>
  <% end %>

  <%= f.text_field :address %>
  <%= f.submit 'Add Link' %>
<% end %>