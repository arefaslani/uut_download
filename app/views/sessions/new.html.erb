<%= bootstrap_form_for :session, url: sessions_url(redirect_url: params['redirect_url']) do |f| %>
  <%= f.text_field :student_number %>
  <%= f.password_field :password %>
  <%= f.submit 'Log in' %>
<% end %>